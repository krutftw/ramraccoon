interface:
  display_name: "RAM Raccoon"
  short_description: "Audit agents and recover runaway memory"
  icon_small: "./assets/ramraccoon-mark.svg"
  icon_large: "./assets/ramraccoon-mark.svg"
  brand_color: "#E2552D"
  default_prompt: "Use $ramraccoon to decide which agents are still needed, check Codex memory pressure, and prepare explicit recovery when required."

policy:
  allow_implicit_invocation: true
